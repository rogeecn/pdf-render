services:
  pdf-library:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./pdfs:/app/pdfs:ro
    environment:
      - PORT=3000
      - PDF_DIR=/app/pdfs
      - SCAN_INTERVAL=1800000
    restart: unless-stopped